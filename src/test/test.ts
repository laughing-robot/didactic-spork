//var el = require("../src/packing/edgeList")
//import 'mocha';
import { EdgeList } from "../packing/edgeList";
//import * from "~packing/strippacker"

let seed = 0;
//create bins and rectangles
//

switch (seed) {
    case 0:
        test1();
        break;

}

function test1() {
    test('two plus two is four', () => {
        expect(2 + 2).toBe(4);
    });
}
